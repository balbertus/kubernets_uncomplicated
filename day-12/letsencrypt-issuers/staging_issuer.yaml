apiVersion: cert-manager.io/v1
kind: Issuer
metadata: 
  name: letsencrypt-staging
spec:
  acme:
    # the ACME server URL
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: bruno.a.s@gmail.com
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
      - http01:
          ingress:
            ingressClassName: nginx
