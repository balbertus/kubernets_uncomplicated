apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-nfsr
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
    volumeMounts:
    - name: balbertus-pvc-nfsr
      mountPath: /usr/share/nginx/html
  volumes:
   - name: balbertus-pvc-nfsr
     persistentVolumeClaim:
       claimName: balbertus-pvc-nfsr
