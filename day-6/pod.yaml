apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
    volumeMounts:
    - name: balbertus-pvc
      mountPath: /usr/share/nginx/html
  volumes:
   - name: balbertus-pvc
     persistentVolumeClaim:
       claimName: balbertus-pvc
